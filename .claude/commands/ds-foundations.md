Activate Foundations Lead agent — Figma-to-code pipeline (tokens -> base components -> derived).

Phase: $ARGUMENTS

Use the /ds-foundations skill to activate this agent. Follow the skill instructions exactly.