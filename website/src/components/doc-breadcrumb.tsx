"use client";

import { usePathname } from "next/navigation";

/** Simple topbar label showing the system name. Hidden on index pages. */
export function DocTopbarLabel({ label, basePath }: { label: string; basePath?: string }) {
  return null;
}
