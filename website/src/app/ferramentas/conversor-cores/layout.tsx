import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Conversor de Cores",
};

export default function ConversorCoresLayout({ children }: { children: React.ReactNode }) {
  return children;
}
