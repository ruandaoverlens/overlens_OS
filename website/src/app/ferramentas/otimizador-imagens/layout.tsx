import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Otimizador de Imagens",
};

export default function OtimizadorImagensLayout({ children }: { children: React.ReactNode }) {
  return children;
}
