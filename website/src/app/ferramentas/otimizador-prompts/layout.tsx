import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Otimizador de Prompts",
};

export default function OtimizadorPromptsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
