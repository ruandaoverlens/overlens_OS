import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Conversor de Formato",
};

export default function ConversorFormatoLayout({ children }: { children: React.ReactNode }) {
  return children;
}
